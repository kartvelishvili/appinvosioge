
# Schema Migrations

This file documents the database schema changes required for the latest features, specifically adding "Boost Mode" tracking and advanced invoice settings (like specific bank accounts, dates, and currency conversions).

## 1. `user_profiles` updates
Added the `boost_enabled` flag to allow users to toggle Boost Mode features across their dashboard and invoice creation.

